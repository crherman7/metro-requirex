# metro-requirex

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build metro-requirex` to build the library.

## Running unit tests

Run `nx test metro-requirex` to execute the unit tests via [Jest](https://jestjs.io).
